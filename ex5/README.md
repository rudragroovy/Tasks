# Exercise 5: Student Management CRUD Application
## Tech Stack
- React
- Node.js
- PostgreSQL

## How to use
Review `implementation.md` and `requirements.md`. Navigate into `student-management-app` to find the codebase. Install dependencies with `npm install` in frontend and backend, then start them.
