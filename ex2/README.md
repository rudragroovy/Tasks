# Exercise 2: Fullstack Application
## Tech Stack
- Frontend (React/Vanilla)
- Backend (Node.js/Express)

## How to use
1. Navigate to the `backend` directory, run `npm install` and `npm start`.
2. Run `node seed.js` to seed the database.
3. Navigate to the `frontend` directory, run `npm install` and `npm start`.
